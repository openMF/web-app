name: Scan AI Agent Skills

on:
  push:
    paths:
      - 'skills/**'
  pull_request:
    paths:
      - 'skills/**'

jobs:
  scan:
    uses: cisco-ai-defense/skill-scanner/.github/workflows/scan-skills.yml@f2858cf3bc1be94e9a51ce0bca9c8d87c64364d7 # main
    with:
      # We tell the scanner to look at the skills directory
      skill_path: skills
    permissions:
      security-events: write
      contents: read
