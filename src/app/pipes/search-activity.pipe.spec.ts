import { SearchActivityPipe } from './search-activity.pipe';

describe('SearchActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
