import { LoanProvisioningAccountFiltering.Pipe.TsPipe } from './loan-provisioning-account-filtering.pipe.ts.pipe';

describe('LoanProvisioningAccountFiltering.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new LoanProvisioningAccountFiltering.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
