import { FetchImagePipe } from './fetch-image.pipe';

describe('FetchImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchImagePipe();
    expect(pipe).toBeTruthy();
  });
});
